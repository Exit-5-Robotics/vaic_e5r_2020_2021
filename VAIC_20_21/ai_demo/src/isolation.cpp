#include "vex.h"

using namespace vex;

void red_isolation() {
  // (x, -y) ball, then (x, 0) ball, then (x, y) ball if time permits
}

void blue_isolation() {
  // (-x, y) ball, then (-x, 0) ball, then (-x, -y) ball if time permits
}